# =============================================================================
# GeoAgent Sandbox — 容器内 Python 依赖
# =============================================================================
# 仅包含代码执行所需的最小依赖集。
# 不包含：streamlit、openai、langchain 等 Web/应用层依赖。
# =============================================================================

# ---- GIS 核心 ----
geopandas>=0.14.0
shapely>=2.0.0
rasterio>=1.3.0
rioxarray>=0.14.0
xarray>=2024.0.0
pyproj>=3.6.0
fiona>=1.9.0
libpysal>=4.9.0
esda>=2.5.0

# ---- 科学计算 ----
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0
scikit-learn>=1.3.0

# ---- 深度学习（可选，按需启用）----
# torch>=2.0.0

# ---- 可视化 ----
matplotlib>=3.7.0
folium>=0.14.0

# ---- 路网分析 ----
osmnx>=1.9.0
networkx>=3.2.0
