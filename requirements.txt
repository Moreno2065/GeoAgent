# =============================================================================
# GeoAgent Python 包依赖
# =============================================================================
# 现代格式的依赖文件，支持选择性安装
# 安装命令:
#   pip install geoagent                  # 最小安装（仅核心功能）
#   pip install geoagent[core]            # 核心功能
#   pip install geoagent[gis]            # GIS 核心库
#   pip install geoagent[viz]            # 可视化
#   pip install geoagent[arcgis]         # ArcGIS 在线数据
#   pip install geoagent[osm]            # 海外网络分析
#   pip install geoagent[all]            # 所有功能（推荐）
#   pip install -e .                     # 开发模式安装
# =============================================================================

# 核心运行时
streamlit>=1.28.0
openai>=1.3.0
python-dotenv>=1.0.0
requests>=2.28.0

# GIS 核心库
geopandas>=0.14.0
shapely>=2.0.0
rasterio>=1.3.0
whitebox>=1.4.0

# 可视化
folium>=0.14.0
leafmap>=0.23.0
matplotlib>=3.7.0
plotly>=5.18.0
streamlit-folium>=0.15.0

# ArcGIS
arcgis>=2.1.0
arcgis-mapping>=0.1.0

# 海外地理分析
osmnx>=1.9.0
networkx>=3.2.0

# 数据处理
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0

# 联网搜索
duckduckgo-search>=5.0.0

# LangChain 沙盒 Agent
langchain-openai>=0.1.0
langchain-core>=0.1.0
langchain-experimental>=0.0.50

# 测试依赖
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
